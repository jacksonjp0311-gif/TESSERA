# Agent Handoff Contract

Before work:

1. Read `README.md`.
2. Read `AGENTS.md`.
3. Read `docs/context/repository_context_index.json`.
4. Read `rcc/nexus/route_map.json`.
5. Read the folder README for the edit target.

After work:

1. Update affected mini README if folder purpose/hooks/surfaces changed.
2. Run validators.
3. Preserve evidence artifacts.
4. Do not push until James authorizes it.

Claim boundary: agent handoff improves context reconstruction only. It is not validation or proof of correctness.
