# Agent CLI Mirror Contract

## Law

```text
No operator-facing script bypasses the Agent CLI Mirror.
No command without a registry entry.
No Worker before Observer.
No failure without a lesson.
No shell-owned loop logic.
```

## Boundary

The mirror is an operator interface and command compression layer. It is not autonomy, safety proof, correctness proof, or transfer proof.