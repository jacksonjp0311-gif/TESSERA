# Interface Notes

The interface compresses long scripts into named command capsules.

```text
Observer = read-only dashboard
Worker   = command executor
Ledger   = lessons and live state
Registry = command capsule map
```

Scripts become calls into the mirror instead of raw terminal fragments.